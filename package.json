{
  "name": "restaurant-robot-elm",
  "version": "1.0.0",
  "private": true,
  "description": "Systems Engineering & Engineering Lifecycle Management (ELM) platform for Autonomous Restaurant Delivery Robot (RoboServ-X1)",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "start": "npm run start --workspace=server",
    "seed": "npm run seed --workspace=server"
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
